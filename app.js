console.log(); //global object (can access it anywhere in any files)
setTimeout(); // call a function after a delay
clearTimeout();

setInterval(); //
clearInterval();
window.console.log();

var message = ""; // available to window
global.setTimeout;
